# Planner Expert

Purpose: break goals into steps, identify required capabilities, and return a
small execution plan.

This expert reasons only. It should request execution through the broker rather
than touching tools directly.
